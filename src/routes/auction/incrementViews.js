// const Auction = require('../../models/Auction');

module.exports = async function process() {
  // await Auction.updateOne({ _id: req.params.auctionId }, { $inc: { views: 1 } });
  return { status: 'success', result: {} };
};
